# SimpleTokenizerV3

这个 bonus notebook 实现了一个保留空白的变体版本，对应章节中的 `SimpleTokenizerV2`。事后看来，这里的 `SimpleTokenizerV3` 可能更适合作为主章节中那个简单分词器更直观的实现。不过为了不偏离原书主线代码，它被单独放在这个 notebook 中。

- [simple-tokenizer-v3.ipynb](simple-tokenizer-v3.ipynb)：一个简单分词器，可以无损地往返（round-trip）保留分隔符和空白