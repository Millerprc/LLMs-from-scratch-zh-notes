# 第 4 章：从零实现一个 GPT 模型来生成文本（Implementing a GPT Model from Scratch To Generate Text）

- [flops-analysis.ipynb](flops-analysis.ipynb) 分析了主章节中实现的 GPT 模型的每秒浮点运算次数（FLOPS）。
- `requirements-extra.txt` 包含需要额外安装的 Python 库（通过 `pip install -r requirements-extra.txt` 安装）。